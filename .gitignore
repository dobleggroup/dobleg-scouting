node_modules
dist
.vercel
.netlify
*.local
.DS_Store
.superpowers/

# Environment variables (NEVER commit these)
.env
.env.local
.env.production
.env.*.local

.worktrees/

# Script run logs (regenerated locally, no lasting value in git)
*.log
__pycache__/

# Local secrets for scripts/*.bat -- NEVER commit (Netlify/GitHub secret
# scanning fails the build/push if this literal value appears in a tracked file)
scripts/secrets.bat
